import React from "react";

const LoadingScreen = () => {
  return (
    <div
      style={{
        height: "80vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        width: "100%",
        // backgroundColor: background,
      }}
    >
      <div>Loading...</div>
    </div>
  );
};

export default LoadingScreen;
